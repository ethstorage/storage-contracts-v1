{
  "name": "ethstorage-storage-contracts",
  "version": "1.0.0",
  "license": "MIT",
  "devDependencies": {
    "snarkjs": "^0.7.5"
  },
  "scripts": {
    "install": "npm install && forge install",
    "compile": "forge build",
    "test": "forge test",
    "clean": "forge clean && rm -rf ./artifacts ./broadcast ./cache ./cache_forge ./tmp",
    "format:check": "forge fmt --check",
    "format:fix": "forge fmt"
  }
}